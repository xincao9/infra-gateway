spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: api
        file-extension: yaml
        group: DEFAULT_GROUP